name: Build Android APK (FINAL)
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          
      - name: Install Core Plugins
        run: |
          npm install --legacy-peer-deps
          # Ставим стабильные официальные плагины для Share и Filesystem
          npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/assets @capacitor/share @capacitor/filesystem
          
      - name: Create Capacitor Config
        run: |
          echo "import { CapacitorConfig } from '@capacitor/cli';
          const config: CapacitorConfig = {
            appId: 'com.evgen.music',
            appName: 'Evgen Music',
            webDir: 'dist',
            server: { androidScheme: 'https' }
          };
          export default config;" > capacitor.config.ts

      - name: Build Web Project
        run: npm run build
        
      - name: Prepare Android & Permissions
        run: |
          npx cap add android
          # Прописываем права на музыку и файлы прямо в системный файл
          sed -i '/<application/i \    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />' android/app/src/main/AndroidManifest.xml
          sed -i '/<application/i \    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />' android/app/src/main/AndroidManifest.xml
          npx cap sync android

      - name: Generate App Icons
        run: |
          mkdir -p assets
          cp icon.png assets/icon.png || echo "No icon"
          npx capacitor-assets generate --android

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: evgen-music-ready
          path: android/app/build/outputs/apk/debug/app-debug.apk
          
